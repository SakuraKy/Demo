package SuanfaTest;

public class nodeadd {
    public static void main(String[] args) {

    }
    /*
     * public void add(int index,E element){
     * 检查索引(index)是否越界
     * checkposition(index);
     * 如果索引等于size，说明是在最后添加
     * if (index == size) {
     * addList(element);
     * return;
     * }
     * 查找索引对应的节点
     * Node<E> p = getNode(index);
     * 此时p就是index位置的节点，需要找到它的前一个节点
     * Node<E> temp = p.prev;
     * 创建一个新的节点，存入element
     * Node<E> x = new Node<>(element);
     * 以下四行代码是将新节点插入到temp和p之间
     * x.prev = temp;
     * x.next = p;
     * p.prev = x;
     * temp.next = x;
     * 
     * 
     * size++;
     * }
     */
}
