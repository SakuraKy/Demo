package SuanfaTest;

public class nodeget {
    public static void main(String[] args) {

    }
    // 通过索引获取链表中对应索引的值（具体是通过索引确定指针位置，在通过指针获取指针对应的值）
    /*
     * public E get(int index) {
     * // 检查索引是否越界
     * checkposition(index);
     * // 查找索引对应的节点
     * Node<E> p = getNode(index);
     * return p.item;
     * }
     */

    // --------------------

    /*
     * public E getFirst(){
     * if (size < 1) {
     * throw new NoSuchElementException();
     * }
     * return head.next.val;
     * }
     */
    // 获取尾部值同理
}
